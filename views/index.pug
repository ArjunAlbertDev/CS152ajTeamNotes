extends layoutBS

block content
  .jumbotron
    h1 TeamNotes
    p Notes for all your different groups!
    //-
      a.btn.btn-danger(href="/auth/google")
        span.fa.fa-google-plus  Google
      br
      | logged in "#{loggedIn}"
      br
      | user: "#{(user&&user.googlename)}"
      br
      || This should be in a comment!
